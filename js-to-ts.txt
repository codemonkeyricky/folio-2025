1. Pick **one** .js source with the least number of lines of code (ie. find sources/ | grep js$  | xargs wc | sort -hsr | tail -n1) 
2. Rename the .js to .ts 
3. Run "npm run build" and fix all errors - this includes fixing all other files that import this file.
4. Make sure no runtime error - run "npm run dev" and ensure no error logs in console
5. Commit the changes
 
